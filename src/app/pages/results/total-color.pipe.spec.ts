import { TotalColorPipe } from './total-color.pipe';

describe('TotalColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalColorPipe();
    expect(pipe).toBeTruthy();
  });
});
